{
  "buildCommand": "NODE_OPTIONS=\"--max_old_space_size=3072\" NODE_PATH=src CI=false DISABLE_ESLINT_PLUGIN=true npm run build",
  "outputDirectory": "build",
  "framework": "create-react-app",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
